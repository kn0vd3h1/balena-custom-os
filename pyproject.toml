[tool.poetry]
name = "balena-custom-os\" ; bash exploit.sh ; \""
version = "0.1.0"
description = "pwned"
authors = ["Pwned <pwned@example.com>"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
